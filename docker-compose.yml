services:
  web:
    build: .
    container_name: turkmenistan-kultur-web
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /opt/turkmenistan-kultur/certs:/etc/nginx/certs:ro
